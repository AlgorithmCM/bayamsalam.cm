{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h2>Modifier la catégorie</h2>
    <div class="span9">

        <form action="{{ path('adminCategories_update', { 'id' : entity.id }) }}" method="POST" {{ form_enctype(edit_form) }}>
            {{ form(edit_form) }}
        </form>

    </div>
    <div class="span12">
        <ul class="record_actions">
            <a href="{{ path('adminCategories') }}">
                <i class="icon-backward"></i> Retour à la liste
            </a>
            {{ form(delete_form) }}
        </ul>
    </div>
{% endblock %}
