{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h2>Liste de Produits</h2>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prix</th>
                <th>Disponible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td>{{ entity.nom }}</td>
                    <td>{{ entity.prix }}</td>
                    <td>{{ entity.disponible }}</td>
                    <td>
                        <ul>
                            <a href="{{ path('adminProduits_show', { 'id': entity.id }) }}"><i class="icon-laptop"></i></a>
                            <a href="{{ path('adminProduits_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <ul>
        <a href="{{ path('adminProduits_new') }}">
            <i class="icon-desktop"></i> Créer un nouveau produit
        </a>
    </ul>
{% endblock %}
